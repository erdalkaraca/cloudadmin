{
  "name": "cloudadmin",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.19.0 || >=22.12.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w cloudadmin",
    "build": "npm run build -w cloudadmin",
    "preview": "npm run preview -w cloudadmin",
    "validate-ci": "node scripts/validate-ci.js"
  },
  "devDependencies": {
    "@rolldown/binding-linux-x64-gnu": "^1.0.2"
  },
  "dependencies": {
  }
}
