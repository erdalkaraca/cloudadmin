{
  "name": "cloudadmin",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": ">=20.19.0 || >=22.12.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bash scripts/with-node.sh npm run dev -w cloudadmin",
    "build": "bash scripts/with-node.sh npm run build -w cloudadmin",
    "preview": "bash scripts/with-node.sh npm run preview -w cloudadmin",
    "install:deps": "bash scripts/with-node.sh npm install"
  },
  "devDependencies": {
    "@rolldown/binding-linux-x64-gnu": "^1.0.2"
  }
}
