export const PROVIDER_ID_COMPANION = 'k8s' as const;
export const PROVIDER_DISPLAY_NAME_COMPANION = 'Kubernetes (Companion + kubectl)';

export const PROVIDER_ID_BEARER = 'k8s-bearer' as const;
export const PROVIDER_DISPLAY_NAME_BEARER = 'Kubernetes (JS API + Bearer Token)';

export const PROVIDER_ID = PROVIDER_ID_COMPANION;
export const PROVIDER_DISPLAY_NAME = 'Kubernetes';
export const COMMAND_NAMESPACE = 'k8s' as const;
